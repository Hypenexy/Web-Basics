var now = new Date();
// You need to then format it if you want to display it, for example:
// var now = today.getHours() + ":" + today.getMinutes() + ":" + today.getSeconds();
// More variants at TimeFormats.js